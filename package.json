{
  "name": "wtenv",
  "version": "0.1.0",
  "description": "Worktree environment manager — per-worktree DNS namespaces, port allocation, and database isolation for Conductor-managed git worktrees",
  "type": "module",
  "bin": {
    "wtenv": "./dist/index.js"
  },
  "exports": {
    ".": "./dist/lib/exports.js"
  },
  "main": "./dist/lib/exports.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "link": "npm run build && npm link",
    "test": "npm run build && node --test test/*.test.mjs && node --import tsx --test \"test/**/*.test.ts\""
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "listr2": "^10.2.2"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
